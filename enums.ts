export enum JenisPilihan {
    rt = "rt",
    rw = "rw"
}

export enum StatusVoting {
    not_ready = "not_ready",
    ready = "ready",
    active = "active",
    done = "done"
}

export enum StatusFormFilling {
    ready = "ready",
    active = "active",
    done = "done"
}